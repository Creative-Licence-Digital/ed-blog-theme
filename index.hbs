{{!< default}}

<header class="masthead {{#if @blog.cover}}" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
  {{navigation}}

  <div class="vertical">
    <div class="masthead-content inner">
      <h1 class="page-title">{{@blog.description}}</h1>
      <h2 class="page-description">
        Keep up to date with the latest Ed news.
      </h2>
    </div>
  </div>
</header>

<main id="content" class="content" role="main">
  {{> "loop"}}
</main>
